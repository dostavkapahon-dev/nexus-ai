# ERRORS — дефекты и их статус

Обновлено: 2026-09-04. Повторный аудит текущей версии, включая вчерашние правки.

## ✅ Исправлено 2026-09-09

### #24 MCP-путь Higgsfield не заработал бы и с токеном
Проверено живым вызовом платформы (мой доступ к Higgsfield MCP): аккаунт `ultimate`,
1200 кредитов, генерация `z_image` прошла за 0.15 кредита и вернула файл. Сравнение
кода с фактическим протоколом вскрыло три несовпадения, каждое из которых само по
себе ломает генерацию:

| Что делал код | Как на самом деле |
|---|---|
| плоские аргументы `{prompt, aspect_ratio, model}` | всё вложено в `params` |
| ждал ссылку на медиа сразу | приходит заявка `{"results":[{"id":…,"status":"pending"}]}`, её надо дождаться через `jobs_wait` |
| передавал `image_url` ссылкой | принимается только `media_id` (`media_import_url`) |

Исправлено в `core/hixiit.py::_generate_via_mcp` вместе с ожиданием задачи
(`_wait_job`) и импортом референса (`_import_media`).

### #25 Каталог моделей был выдуман из старого README
В коде стояли `soul` и `dop-lite/turbo/standard` — я взял их из README SDK
`@higgsfield/client`. В каталоге аккаунта таких моделей нет. Реальные:
изображения — `z_image`, `soul_2`, `gpt_image_2`, `cinematic_studio_2_5`,
`marketing_studio_image`, `image_auto`; видео — `minimax_hailuo`,
`cinematic_studio_3_0`, `flux_3_video`, `marketing_studio_video`, `wan2_6`.

Выбор модели стал правилом «задача → модель», а не списком: товар → Marketing
Studio, текст в кадре → GPT Image 2, человек → Soul 2, черновик → Z Image
(дёшево), видео без исходного кадра → FLUX 3 (умеет text-to-video), с кадром →
Minimax Hailuo. Промпт адаптируется под модель: Soul не нужно переописывать лицо,
GPT Image нужен точный текст, Hailuo — движение и эмоция.

### #26 Безлимит не использовался вообще
Платформа принимает `use_unlim`, а список покрытых моделей отдаёт
`models_explore(unlim=true)`. Ни того, ни другого в коде не было: генерация всегда
шла за кредиты. Теперь безлимит тратится, когда он есть **и** покрывает выбранную
модель (иначе платформа отклонит запрос), и виден в `/hixiit` отдельной строкой.

Закреплено тестами `backend/tests/test_higgsfield_expert.py` (15 проверок).

## ✅ Исправлено 2026-09-05

### #23 «Пропадёт при деплое» врало при подключённой базе
`/diag` подписывал так любой ключ, взятый из базы, не глядя на то, какое
хранилище подключено. С внешней Postgres такой ключ никуда не пропадает, а
приписка заставляла настраивать заново уже настроенное — и меня саму привела к
неверному выводу «DATABASE_URL не подключён».

Теперь пометка зависит от `storage_info().persistent`: при постоянной базе —
просто «база», при временном диске предупреждение остаётся.

Закреплено тестами `backend/tests/test_storage_label.py` (2 проверки).

### #22 Поиск отвечал «пусто» вместо причины
В диагностике: «ни один источник не дал результатов (perplexity: пусто;
duckduckgo: пусто)». На деле причины разные и лечатся по-разному: у Perplexity
не задан ключ, а DuckDuckGo отдаёт 403 на запросы с серверных адресов. Из
сообщения этого было не понять.

Исправление:
* `duckduckgo.search_detailed()` — два адреса (html и lite), POST и GET, разбор
  несколькими шаблонами вёрстки, разворачивание редиректа `uddg=` в настоящую
  ссылку. Раньше один жёсткий шаблон при смене вёрстки давал молчаливое «пусто».
* Причина отказа поднимается наверх словами: «нет ключа PERPLEXITY_API_KEY»,
  «HTTP 403 (запросы с сервера блокируются)», «проверка на робота».

Закреплено тестами `backend/tests/test_search_reasons.py` (5 проверок).

### #21 «CONTROL_FEED задан и в дашборде, и в хостинге» — ложная тревога
Таблица `connections` служит и хранилищем доступов, и KV-складом внутреннего
состояния: лента Командного центра (`control_feed`), история диалога, состояние
автопилота, выбранные модели. Загрузчик выгружал в переменные окружения всё
подряд, а лента меняется каждую минуту — её новое значение выглядело как
«ключ задан в двух местах». Подсказка при этом советовала удалить запись в
Подключениях, то есть стереть ленту событий.

Исправление:
* `credentials.is_credential()` — в окружение попадают только доступы: поля из
  `FIELDS` и незнакомые имена с окончаниями ключей. Состояние остаётся в базе.
* Загрузчик помнит, что положил в окружение сам, и не считает конфликтом
  собственное прошлое значение. Раньше любая правка ключа в дашборде при второй
  загрузке выглядела как перекрытие переменной хостинга.

Настоящее перекрытие (значение в Render и другое в дашборде) по-прежнему
показывается — это проверено отдельным тестом.

Закреплено тестами `backend/tests/test_credentials_are_not_state.py` (6 проверок).

### #20 «Работает, только пока открыт сайт» — сервис засыпал
Telegram-бот работает long-polling'ом внутри серверного процесса. Бесплатный
Render останавливает сервис после ~15 минут без входящих запросов — вместе с ним
замолкают бот, планировщик и очередь публикаций. Просыпался сервис, только когда
кто-то открывал адрес. Снаружи это выглядело буквально как «пока не зайдёшь на
сайт, ничего не работает», хотя фронтенд тут ни при чём.

Исправление: `core/keepalive.py` — сервис раз в 10 минут дёргает собственный
`/api/health`. Запрос уходит наружу и возвращается как входящий, а именно его
Render считает активностью. Состояние видно в `/diag`; выключается переменной
`NEXUS_KEEPALIVE=off` (платный тариф или внешний UptimeRobot).

Закреплено тестами `backend/tests/test_keepalive.py` (5 проверок).

### #18 Модель для генерации нельзя было выбрать без MCP
`/model` отвечал «Модели HIXIIT недоступны», хотя ключ Higgsfield рабочий и
генерация шла: список моделей брался только из каталога MCP. Теперь к нему
добавлены модели REST-пути — Soul для изображений, dop-lite/turbo/standard для
видео, — и они помечаются доступными, только если есть ключ и секрет.

Главное: выбранная модель теперь реально доезжает до запроса. Раньше REST-путь
брал `HIGGSFIELD_MODEL` из окружения, и ручной выбор был украшением.

### #19 Ошиблись хостом — выглядело как «не работает»
В документации и SDK Higgsfield встречаются два адреса: `platform.higgsfield.ai`
и `api.higgsfield.ai`. Неверный хост отвечает 404, и это неотличимо от «сервис
сломан». Теперь при 404 автоматически пробуется второй адрес, рабочий
запоминается на время работы процесса, а `HIGGSFIELD_API_BASE` по-прежнему
перекрывает оба.

### #17 «Ключ есть и в Render, и в дашборде» — а генерации нет
Значение из дашборда перекрывает переменную хостинга (это верно: человек правит
именно в дашборде). Но раньше об этом говорила одна строка в логе Render при
старте. Со стороны выглядело так: положил новый ключ в Render — ничего не
изменилось, и чинить человек шёл не туда.

Теперь `/hixiit` показывает по каждой половине доступа: заполнена ли, откуда
(дашборд / переменная хостинга / дашборд перекрывает Render) и последние 4
символа — чтобы сверить с Render, не раскрывая ключ. При перекрытии выводится
прямое предупреждение с тем, что делать.

Закреплено тестами `backend/tests/test_key_source_visible.py` (4 проверки).

### #15 Higgsfield не генерировал ничего — выдуманные адреса API
Главная причина «не генерируется в Higgsfield». REST-клиент обращался к
`POST /v1/text2video` и `GET /v1/jobs/{id}` — таких эндпоинтов у платформы нет,
поэтому путь не мог заработать ни при каких ключах.

Сверено с официальным SDK `@higgsfield/client`. Настоящий API:

| Что | Как на самом деле | Было |
|---|---|---|
| База | `https://platform.higgsfield.ai` | `.../v1` |
| Доступ | `hf-api-key` + `hf-secret` (v1), `Authorization: Key k:s` (v2) | только `Authorization` |
| Картинка | `POST /v1/text2image/soul`, тело `{"params": {...}}` | не поддерживалась |
| Видео | `POST /v1/image2video/dop` | `POST /v1/text2video` |
| Ожидание | `GET /v1/job-sets/{id}` → `jobs[].results.raw.url` | `GET /v1/jobs/{id}` |
| Размер | разрешение `1152x2048` | строка `9:16` |

Дополнительно: DoP — это image2video, ему нужен исходный кадр. Поэтому «видео из
текста» теперь идёт штатным путём Higgsfield: Soul рисует кадр → DoP оживляет.
Картинки больше не уезжают на бесплатный Pollinations при живом ключе.

### #16 «Ключ есть» показывался как рабочее подключение
`/hixiit` и проверка здоровья ставили ✅ на факт наличия ключа. Ключ бывает от
другого аккаунта, без кредитов или без секрета — генерация при этом не работает.
Добавлен `higgsfield.check()` — настоящий запрос к платформе; ✅ теперь означает
«запрос прошёл», иначе показывается причина (401 / 403 / 422).

Заодно ключ без секрета больше не считается доступом: раньше конвейер выбирал
Higgsfield и падал на 401 вместо того, чтобы уйти на запасной путь.

Закреплено тестами `backend/tests/test_higgsfield_api.py` (7 проверок).

### #13 Разбор аккаунта никуда не сохранялся (ТЗ §20)
Анализ Instagram отрабатывал по-настоящему (Graph API → Bright Data → браузер),
но результат жил ровно одно сообщение в чате. На следующем шаге стратег и
копирайтер снова работали вслепую, а на вопрос «что у меня с аккаунтом»
отвечать было нечем.

Исправление: `research_store.remember_account` кладёт разбор в ту же историю
исследований (`kind="account"`), с защитой от засорения — один и тот же аккаунт
не пишется чаще раза в 6 часов. Сохранение подключено в общей точке
`social_intel.get_account_intelligence` и в инструменте `analyze` дирижёра.
Память подставляется в промпты через `research_store.market_context`
(блок «МОИ АККАУНТЫ»).

### #14 Бот не помнил нишу в разговоре (ТЗ §22)
Ниша и профиль сохранялись в базе и переживали рестарт, но в свободный ответ
(`intent.chat_reply`) не попадали: на «какая у меня ниша?» модель отвечала
догадкой. Теперь `intent.memory_block()` подставляет профиль агента и свежие
разборы аккаунтов в системный промпт — и в обычном пути, и в ручном (эскроу).

Закреплено тестами `backend/tests/test_account_memory.py` (6 проверок).

### #10 «Нет ни одной модели» при живых ключах — главный симптом
Свободный текст в Telegram отвечал «нет ни одной модели для анализа», хотя в
Подключениях ключи стояли. Причина: окружение процесса обновлялось из базы
**только в обработчике слэш-команд** (`_dispatch_command`). Ключ, добавленный в
дашборде после старта сервера, свободному тексту и разбору медиа был не виден —
`ai_available()` возвращала False.

Исправление: `_handle_plain_text` и `_handle_media` тоже подтягивают ключи.
Тесты `tests/test_keys_reach_all_paths.py` проверены на откате правки: без неё
падают, с ней проходят.

### #11 `/strategy` падал вместо объяснения
Единственная из 30 команд, которая при отсутствии ключей завершалась
необработанным исключением: человек видел «Анализирую...» и больше ничего.
Теперь причина объясняется, как и в остальных командах.

### #12 Диагностика молчала о временном хранилище
`/diag` открывают именно тогда, когда «ключи опять слетели», но про эфемерность
хранилища говорил только `/status`. Теперь `/diag` называет причину и лечение.

### Аудит Telegram
Прогнаны все 30 команд: каждая отвечает, ни одна не падает и не молчит.
Закреплено тестом `tests/test_all_commands_answer.py`.

## 🔴 Открытые

### #1 Память и ключи стираются при каждом рестарте
`database/db.py` без `DATABASE_URL` использует SQLite-файл, а в `render.yaml`
диск не подключён. Файловая система Render эфемерна, поэтому при деплое или
пробуждении сервиса пропадает **всё**: ниши, стратегии, контент-план, очередь,
созданный контент, история задач и **сохранённые API-ключи** из таблицы
`connections`.

Это и есть корень жалобы «отваливаются ключи»: сами ключи хранятся правильно
(шифруются в `core/secrets.py`, не попадают в код), но переживают ровно до
следующего рестарта.

Код к постоянной базе готов: `normalize_db_url` приводит `postgres://` и
`postgresql://` к asyncpg и чинит параметры `sslmode`/`pgbouncer`, есть
alembic-миграции. **Осталось действие владельца** — TODO #1.

### #2 HIXIIT не подключён на сервере
Нужны ключ и секрет из cloud.higgsfield.ai. До этого генерация идёт по запасным
путям. TODO #2.

## ✅ Исправлено сегодня

### #3 Контент-план не превращался в публикации
`/plan7` строил план недели и складывал его в KV-состояние автопилота
(`autopilot_state.week_plan`), где его **никто не читал**. Генерация, очередь,
публикация и планировщик работают с таблицей `content_plans`, которую заполнял
только старый сценарий `/analyze`. Получались два несвязанных контент-плана:
один видно в чате, но из него нельзя создать публикацию; второй умеет всё, но
требует другого входа.

Исправление: `autopilot.persist_week_plan()` сохраняет план в `content_plans`
с привязкой к активной нише. Формат подсказывает площадку (reels → instagram,
post → telegram), но выбор ограничен площадками, подключёнными для этой ниши.
Повторный `/plan7` заменяет только незапущенные пункты — уже сгенерированное
не затирается. Тесты: `tests/test_content_plan_link.py`.

### #4 `/plan7` требовал заново проходить интервью
Команда упиралась в «Сначала пройди /auto», даже когда стратегия уже была
выбрана и сохранена в `strategy_store` (а она, в отличие от состояния
автопилота, переживает перезапуск). Теперь при пустом состоянии берётся
сохранённая стратегия.

## ✅ Исправлено вчера

- **#5 Схема авторизации Higgsfield.** Уходил `Authorization: Bearer <ключ>` и
  заголовок `hf-secret`, тогда как API ждёт `Authorization: Key <ключ>:<секрет>`.
  REST-путь не мог заработать ни при каких настройках — поэтому производство
  видео и отдали внешнему исполнителю.
- **#6 Cloud Code был доступен только с сайта.** Дирижёр вызывался из веб-роутов;
  при выключенном сайте сложная задача из Telegram никуда не доходила. Добавлен
  маршрут `/director`, переиспользующий `command_center.run_command`.
- **#7 Медиа не доходили до чата.** Шаги дирижёра не несли ссылку на результат.
- **#8 HIXIIT: модель бралась из хардкода**, каталог аккаунта не спрашивался.
- **#9 `pyo3 PanicException`** от сломанной сборки `mcp` не наследуется от
  `Exception` и проходила сквозь `except`, унося задачу целиком.

## ❌ Не подтвердилось при проверке

Эти жалобы проверены на текущем коде и **не воспроизводятся** — переделывать
здесь нечего:

- «Telegram зависает во время генерации» — все обработчики запускаются через
  `asyncio.create_task`, приём сообщений не блокируется.
- «Отваливаются запросы по таймауту» — таймаут задан во всех 65 вызовах
  `httpx.AsyncClient`.
- «Сайт перегружен, много лишних страниц» — разделов 7, и все шесть требуемых
  ТЗ на месте; 23 файла страниц — это вкладки внутри разделов.
- «Мёртвый код на фронте» — каждая страница импортируется: соседние страницы
  подключают друг друга как вкладки (`./PromptStudio`, а не `pages/...`).
